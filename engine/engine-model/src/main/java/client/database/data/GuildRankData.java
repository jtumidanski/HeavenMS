package client.database.data;

public record GuildRankData(String name, Integer gp, Integer logo, Integer logoColor, Integer logoBackground,
                            Integer logoBackgroundColor) {
}
